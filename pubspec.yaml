name: project_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase dependencies
  firebase_core: ^2.15.1
  firebase_auth: ^4.2.7
  cloud_firestore: ^4.7.1
  firebase_storage: ^11.2.7
  firebase_messaging: ^14.6.7
  firebase_dynamic_links: ^5.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
